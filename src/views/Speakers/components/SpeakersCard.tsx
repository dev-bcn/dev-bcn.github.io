import {FC} from 'react';

type SpeakersCardProps = {
    title: string;
    subtitle: string;
    text: string;
 }

export const SpeakersCard: FC<SpeakersCardProps> = ({title, subtitle, text}) => {
     return (<p>
         hola
</p>)
}
